/** Width / height — supporting visual above workout text (not photo-primary). */
export const ENTRY_POST_IMAGE_ASPECT = 3 / 2;

/** Same ratio string for CSS / Chakra `aspect-ratio`; keep in sync with `ENTRY_POST_IMAGE_ASPECT`. */
export const ENTRY_POST_MEDIA_ASPECT = "3/2";

/** Square avatars */
export const PROFILE_IMAGE_ASPECT = 1;
